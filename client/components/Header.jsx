import React from 'react';

import { AppBar, AppTitle } from './styled_components';

const Header = () => (
  <header>
    <AppBar>
      <AppTitle>Comidas Gostosas</AppTitle>
    </AppBar>
  </header>
);

export default Header;
